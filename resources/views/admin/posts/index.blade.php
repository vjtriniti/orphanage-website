@extends('layouts.admin')
@section('title','Content')
@section('content')<div class="space-y-6"><div><h1 class="text-2xl font-bold">News & Content</h1><p class="text-slate-500">Publish updates and manage website stories.</p></div>@if(session('success'))<div class="rounded-xl bg-emerald-50 p-4 text-emerald-700">{{session('success')}}</div>@endif<form method="POST" action="{{route('admin.posts.store')}}" class="grid gap-3 rounded-2xl bg-white p-5 shadow-sm">@csrf<input name="title" required placeholder="Post title" class="rounded border p-3"><textarea name="excerpt" placeholder="Short excerpt" class="rounded border p-3"></textarea><textarea name="content" required placeholder="Full content" rows="8" class="rounded border p-3"></textarea><select name="status" class="rounded border p-3"><option value="draft">Draft</option><option value="published">Published</option></select><button class="rounded bg-emerald-700 px-5 py-3 text-white">Publish / save</button></form><div class="rounded-2xl bg-white shadow-sm">@forelse($posts as $p)<div class="flex justify-between gap-4 border-b p-5"><div><h2 class="font-bold">{{$p->title}}</h2><p class="text-sm text-slate-500">{{$p->status}}</p></div><form method="POST" action="{{route('admin.posts.destroy',$p)}}">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></div>@empty<div class="p-8 text-center text-slate-500">No posts yet.</div>@endforelse</div>{{$posts->links()}}</div>@endsection
