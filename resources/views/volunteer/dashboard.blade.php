@extends('layouts.app')
@section('content')
<div class="min-h-screen bg-slate-50 px-4 py-10"><div class="mx-auto max-w-6xl"><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-xs font-bold uppercase tracking-widest text-emerald-700">Volunteer Portal</p><h1 class="mt-2 text-3xl font-black">Welcome, {{ auth()->user()->name }}</h1><p class="mt-2 text-slate-500">Track your application, sessions and volunteer impact.</p></div><a href="{{ route('dashboard') }}" class="rounded-xl border bg-white px-5 py-3 text-center text-sm font-bold">Donor dashboard</a></div>@if(session('success'))<div class="mt-5 rounded-xl bg-emerald-50 p-4 text-sm font-semibold text-emerald-800">{{ session('success') }}</div>@endif
<div class="mt-7 grid gap-5 sm:grid-cols-3"><div class="rounded-2xl bg-white p-6 ring-1 ring-slate-200"><p class="text-sm text-slate-500">Application status</p><p class="mt-2 text-2xl font-black capitalize">{{ $volunteer->status }}</p></div><div class="rounded-2xl bg-white p-6 ring-1 ring-slate-200"><p class="text-sm text-slate-500">Approved hours</p><p class="mt-2 text-2xl font-black">{{ number_format((float)$total,2) }}</p></div><div class="rounded-2xl bg-white p-6 ring-1 ring-slate-200"><p class="text-sm text-slate-500">Sessions</p><p class="mt-2 text-2xl font-black">{{ $hours->total() }}</p></div></div>
<div class="mt-6 grid gap-6 lg:grid-cols-3"><section class="rounded-2xl bg-white p-6 ring-1 ring-slate-200 lg:col-span-2"><div class="flex items-center justify-between"><div><h2 class="text-lg font-black">Volunteer sessions</h2><p class="text-sm text-slate-500">Check in when you begin and check out when you finish.</p></div>@if($volunteer->status==='approved')<form method="POST" action="{{ route('volunteer.checkin') }}">@csrf<button class="rounded-xl bg-emerald-800 px-4 py-2 text-sm font-bold text-white">Check in</button></form>@endif</div><div class="mt-5 space-y-3">@forelse($hours as $hour)<div class="flex flex-col gap-3 rounded-xl bg-slate-50 p-4 sm:flex-row sm:items-center sm:justify-between"><div><p class="font-bold">{{ $hour->activity ?: 'Volunteer session' }}</p><p class="text-xs text-slate-500">Started {{ optional($hour->started_at)->format('d M Y, g:i A') }}</p></div><div class="text-right">@if($hour->ended_at)<p class="font-black">{{ number_format((float)$hour->hours,2) }} hrs</p><p class="text-xs text-slate-500">Completed {{ $hour->ended_at->format('g:i A') }}</p>@else<form method="POST" action="{{ route('volunteer.checkout',$hour) }}">@csrf @method('PATCH')<button class="rounded-lg bg-amber-400 px-3 py-2 text-xs font-black text-emerald-950">Check out</button></form>@endif</div></div>@empty<p class="py-10 text-center text-slate-500">No volunteer sessions yet.</p>@endforelse</div><div class="mt-5">{{ $hours->links() }}</div></section><aside class="rounded-2xl bg-emerald-950 p-6 text-white"><h2 class="text-lg font-black">Your application</h2><dl class="mt-5 space-y-4 text-sm"><div><dt class="text-emerald-300">Skills</dt><dd class="mt-1">{{ $volunteer->skills ?: 'Not provided' }}</dd></div><div><dt class="text-emerald-300">Availability</dt><dd class="mt-1">{{ $volunteer->availability ?: 'Not provided' }}</dd></div><div><dt class="text-emerald-300">Experience</dt><dd class="mt-1">{{ $volunteer->experience ?: 'Not provided' }}</dd></div></dl></aside></div></div></div>
@endsection
