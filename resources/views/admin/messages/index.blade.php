@extends('layouts.admin')
@section('title','Messages')
@section('content')<div class="space-y-6"><div><h1 class="text-2xl font-bold">Contact Inbox</h1><p class="text-slate-500">Manage messages received from the public website.</p></div>@if(session('success'))<div class="rounded-xl bg-emerald-50 p-4 text-emerald-700">{{session('success')}}</div>@endif<div class="space-y-3">@forelse($messages as $m)<div class="rounded-2xl bg-white p-5 shadow-sm"><div class="flex flex-wrap justify-between gap-2"><div><h2 class="font-bold">{{$m->name}} · {{$m->email}}</h2><p class="text-sm text-slate-500">{{$m->subject}}</p></div><span class="text-xs">{{$m->is_read?'Read':'Unread'}}</span></div><p class="mt-3">{{$m->message}}</p><div class="mt-4 flex gap-4"><form method="POST" action="{{route('admin.messages.read',$m)}}">@csrf @method('PATCH')<button class="text-emerald-700">Mark read</button></form><form method="POST" action="{{route('admin.messages.destroy',$m)}}">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></div></div>@empty<div class="rounded-2xl bg-white p-8 text-center text-slate-500">Inbox is empty.</div>@endforelse</div>{{$messages->links()}}</div>@endsection
