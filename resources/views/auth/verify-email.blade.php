@extends('auth.layout') @section('title','Verify email') @section('content')<div class="text-center"><div class="mx-auto flex h-16 w-16 items-center justify-center rounded-2xl bg-emerald-100 text-2xl">✉</div><h2 class="mt-6 text-3xl font-black">Verify your email</h2><p class="mt-3 text-slate-500">We sent a verification link to your email address. Verify it to unlock your account.</p><form method="POST" action="{{ route('verification.send') }}" class="mt-6">@csrf<button class="font-bold text-emerald-700">Resend verification email</button></form><form method="POST" action="{{ route('logout') }}" class="mt-4">@csrf<button class="text-sm text-slate-500">Sign out</button></form></div>@endsection