@extends('layouts.app')
@section('content')
<div class="min-h-screen bg-slate-50 px-4 py-8"><div class="mx-auto max-w-6xl"><div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><p class="text-sm font-semibold text-emerald-700">DONOR PORTAL</p><h1 class="text-3xl font-bold text-slate-900">Recurring giving</h1><p class="mt-1 text-slate-500">Manage your recurring donation preferences.</p></div><a href="{{ route('dashboard') }}" class="rounded-xl bg-emerald-800 px-4 py-2 text-sm font-semibold text-white">Back to dashboard</a></div>
@if(session('success'))<div class="mt-5 rounded-xl bg-emerald-50 p-4 text-sm text-emerald-800">{{ session('success') }}</div>@endif
<div class="mt-6 grid gap-6 lg:grid-cols-3"><form method="POST" action="{{ route('donor.recurring.store') }}" class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200 lg:col-span-1">@csrf<h2 class="font-bold text-slate-900">New schedule</h2><label class="mt-5 block text-sm font-medium">Amount<input name="amount" type="number" min="100" step="0.01" required class="mt-2 w-full rounded-xl border-slate-200"></label><label class="mt-4 block text-sm font-medium">Currency<select name="currency" class="mt-2 w-full rounded-xl border-slate-200"><option>NGN</option><option>USD</option></select></label><label class="mt-4 block text-sm font-medium">Frequency<select name="frequency" class="mt-2 w-full rounded-xl border-slate-200"><option value="monthly">Monthly</option><option value="weekly">Weekly</option><option value="quarterly">Quarterly</option></select></label><button class="mt-6 w-full rounded-xl bg-emerald-800 px-4 py-3 font-semibold text-white">Save preference</button></form>
<div class="space-y-4 lg:col-span-2">@forelse($recurring as $item)<div class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><p class="text-2xl font-bold text-slate-900">{{ $item->currency }} {{ number_format((float)$item->amount,2) }}</p><p class="text-sm capitalize text-slate-500">{{ $item->frequency }} · {{ str_replace('_',' ', $item->status) }}</p></div>@if($item->status !== 'cancelled')<form method="POST" action="{{ route('donor.recurring.cancel',$item) }}">@csrf @method('DELETE')<button class="rounded-xl border border-red-200 px-4 py-2 text-sm font-semibold text-red-700">Cancel</button></form>@endif</div></div>@empty<div class="rounded-2xl bg-white p-10 text-center text-slate-500 ring-1 ring-slate-200">No recurring donation schedules yet.</div>@endforelse</div></div></div></div>
@endsection
