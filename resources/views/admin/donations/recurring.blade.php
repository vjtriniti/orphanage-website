@extends('layouts.admin')
@section('content')
<div class="space-y-6"><div><p class="text-xs font-bold uppercase tracking-widest text-emerald-600">Donations</p><h1 class="text-3xl font-black">Recurring Donations</h1><p class="text-slate-500">Monitor recurring giving preferences and payment setup status.</p></div>@if(session('success'))<div class="rounded-xl bg-emerald-50 p-4 text-sm text-emerald-800">{{ session('success') }}</div>@endif<div class="overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-6 py-4">Donor</th><th class="px-6 py-4">Amount</th><th class="px-6 py-4">Frequency</th><th class="px-6 py-4">Provider</th><th class="px-6 py-4">Status</th><th class="px-6 py-4">Action</th></tr></thead><tbody class="divide-y">@forelse($recurring as $item)<tr><td class="px-6 py-4"><b>{{ $item->email }}</b></td><td class="px-6 py-4 font-bold">{{ $item->currency }} {{ number_format((float)$item->amount,2) }}</td><td class="px-6 py-4 capitalize">{{ $item->frequency }}</td><td class="px-6 py-4 capitalize">{{ $item->provider ?: '—' }}</td><td class="px-6 py-4 capitalize">{{ str_replace('_',' ', $item->status) }}</td><td class="px-6 py-4">@if($item->status !== 'cancelled')<form method="POST" action="{{ route('admin.recurring.cancel',$item) }}">@csrf @method('PATCH')<button class="font-bold text-red-700">Cancel</button></form>@else<span class="text-slate-400">—</span>@endif</td></tr>@empty<tr><td colspan="6" class="px-6 py-12 text-center text-slate-500">No recurring donation records.</td></tr>@endforelse</tbody></table></div><div class="border-t p-5">{{ $recurring->links() }}</div></div></div>
@endsection
