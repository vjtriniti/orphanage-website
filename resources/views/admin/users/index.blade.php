@extends('layouts.admin')
@section('title','Users')
@section('content')
<div class="space-y-6"><div><h1 class="text-2xl font-bold">Users & Staff</h1><p class="text-slate-500">Manage accounts and administrator access.</p></div>
@if(session('success'))<div class="rounded-xl bg-emerald-50 p-4 text-emerald-700">{{session('success')}}</div>@endif @if(session('error'))<div class="rounded-xl bg-red-50 p-4 text-red-700">{{session('error')}}</div>@endif
<div class="overflow-x-auto rounded-2xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="bg-slate-50"><tr><th class="p-4">User</th><th>Roles</th><th>Admin</th><th class="p-4">Actions</th></tr></thead><tbody>@forelse($users as $u)<tr class="border-t align-top"><td class="p-4"><b>{{$u->name}}</b><div class="text-slate-500">{{$u->email}}</div></td><td>{{ $u->roles->pluck('label')->join(', ') ?: 'No role' }}</td><td>{{$u->is_admin?'Yes':'No'}}</td><td class="p-4"><details><summary class="cursor-pointer text-emerald-700">Edit</summary><form method="POST" action="{{route('admin.users.update',$u)}}" class="mt-3 grid min-w-[280px] gap-2">@csrf @method('PUT')<input name="name" value="{{$u->name}}" class="rounded border p-2" required><input name="email" type="email" value="{{$u->email}}" class="rounded border p-2" required><input name="password" type="password" placeholder="New password (optional)" class="rounded border p-2"><label><input type="checkbox" name="is_admin" value="1" @checked($u->is_admin)> Admin access</label><button class="rounded bg-slate-900 px-3 py-2 text-white">Update</button></form><a href="{{route('admin.users.roles.edit',$u)}}" class="mt-2 inline-block text-emerald-700">Manage roles</a><form method="POST" action="{{route('admin.users.destroy',$u)}}" class="mt-2">@csrf @method('DELETE')<button class="text-red-600" onclick="return confirm('Delete this user?')">Delete</button></form></details></td></tr>@empty<tr><td colspan="4" class="p-8 text-center text-slate-500">No users found.</td></tr>@endforelse</tbody></table></div>{{$users->links()}}</div>
@endsection
