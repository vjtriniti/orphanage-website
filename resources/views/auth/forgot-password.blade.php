@extends('auth.layout') @section('title','Reset password') @section('content')<h2 class="text-3xl font-black">Reset your password</h2><p class="mt-2 text-slate-500">Enter your email and we'll send a reset link.</p><form method="POST" action="{{ route('password.email') }}" class="mt-8 space-y-5">@csrf<input name="email" type="email" value="{{ old('email') }}" required placeholder="you@example.com" class="w-full rounded-xl border border-slate-200 px-4 py-3"><button class="w-full rounded-xl bg-emerald-700 px-4 py-3 font-bold text-white">Send reset link</button></form><p class="mt-6 text-center"><a class="font-bold text-emerald-700" href="{{ route('login') }}">Back to sign in</a></p>@endsection