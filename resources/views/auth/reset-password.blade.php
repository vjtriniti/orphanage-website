@extends('auth.layout') @section('title','Choose new password') @section('content')<h2 class="text-3xl font-black">Choose a new password</h2><p class="mt-2 text-slate-500">Create a new secure password for your account.</p><form method="POST" action="{{ route('password.update') }}" class="mt-8 space-y-5">@csrf<input type="hidden" name="token" value="{{ $token }}"><input name="email" type="email" value="{{ old('email',$email) }}" required class="w-full rounded-xl border border-slate-200 px-4 py-3"><input name="password" type="password" required placeholder="New password" class="w-full rounded-xl border border-slate-200 px-4 py-3"><input name="password_confirmation" type="password" required placeholder="Confirm password" class="w-full rounded-xl border border-slate-200 px-4 py-3"><button class="w-full rounded-xl bg-emerald-700 px-4 py-3 font-bold text-white">Reset password</button></form>@endsection