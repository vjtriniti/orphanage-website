@extends('layouts.admin')
@section('title','Events')
@section('content')<div class="space-y-6"><div><h1 class="text-2xl font-bold">Events</h1><p class="text-slate-500">Publish upcoming community and fundraising events.</p></div>@if(session('success'))<div class="rounded-xl bg-emerald-50 p-4 text-emerald-700">{{session('success')}}</div>@endif<form method="POST" action="{{route('admin.events.store')}}" enctype="multipart/form-data" class="grid gap-3 rounded-2xl bg-white p-5 shadow-sm md:grid-cols-2">@csrf<input name="title" required placeholder="Event title" class="rounded border p-3"><input name="location" placeholder="Location" class="rounded border p-3"><input name="starts_at" type="datetime-local" required class="rounded border p-3"><input name="ends_at" type="datetime-local" class="rounded border p-3"><input name="image" type="file" accept="image/*" class="rounded border p-3"><textarea name="description" required placeholder="Description" class="rounded border p-3 md:col-span-2"></textarea><label><input type="checkbox" name="published" value="1"> Publish event</label><button class="rounded bg-emerald-700 px-5 py-3 text-white">Create event</button></form><div class="grid gap-4 md:grid-cols-2">@forelse($events as $e)<div class="rounded-2xl bg-white p-5 shadow-sm"><h2 class="font-bold">{{$e->title}}</h2><p class="text-sm text-slate-500">{{$e->starts_at}} · {{$e->location}}</p><p class="mt-2">{{$e->description}}</p><form method="POST" action="{{route('admin.events.destroy',$e)}}" class="mt-4">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></div>@empty<div class="rounded-2xl bg-white p-8 text-center text-slate-500">No events.</div>@endforelse</div>{{$events->links()}}</div>@endsection
