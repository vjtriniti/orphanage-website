@extends('layouts.admin')
@section('title','Inventory')
@section('content')<div class="space-y-6"><div><h1 class="text-2xl font-bold">Inventory</h1><p class="text-slate-500">Track supplies and every stock movement.</p></div>@if(session('success'))<div class="rounded-xl bg-emerald-50 p-4 text-emerald-700">{{session('success')}}</div>@endif<div class="grid gap-4 md:grid-cols-2">@forelse($items as $item)<div class="rounded-2xl bg-white p-5 shadow-sm"><div class="flex justify-between"><div><h2 class="font-bold">{{$item->name}}</h2><p class="text-sm text-slate-500">{{$item->category->name ?? 'Uncategorized'}}</p></div><span class="text-xl font-bold">{{$item->quantity}}</span></div><form method="POST" action="{{route('admin.inventory.move',$item)}}" class="mt-4 flex flex-wrap gap-2">@csrf<select name="type" class="rounded border p-2"><option value="in">Stock in</option><option value="out">Stock out</option><option value="adjustment">Set quantity</option></select><input name="quantity" type="number" min="0.01" step="0.01" required placeholder="Qty" class="w-28 rounded border p-2"><input name="reason" placeholder="Reason" class="flex-1 rounded border p-2"><button class="rounded bg-emerald-700 px-3 py-2 text-white">Update</button></form></div>@empty<div class="rounded-2xl bg-white p-8 text-center text-slate-500">No inventory items.</div>@endforelse</div>{{$items->links()}}</div>@endsection
