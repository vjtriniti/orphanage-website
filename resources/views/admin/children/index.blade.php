@extends('layouts.admin')
@section('title','Children')
@section('content')
<div class="space-y-6"><div><h1 class="text-2xl font-bold">Children</h1><p class="text-slate-500">Safeguarded profiles using public codes rather than sensitive identity data.</p></div>@if(session('success'))<div class="rounded-xl bg-emerald-50 p-4 text-emerald-700">{{session('success')}}</div>@endif
<form method="POST" action="{{route('admin.children.store')}}" class="grid gap-3 rounded-2xl bg-white p-5 shadow-sm md:grid-cols-2">@csrf<input name="public_code" required placeholder="Public code" class="rounded-lg border p-3"><input name="age" type="number" min="0" max="18" placeholder="Age" class="rounded-lg border p-3"><select name="gender" class="rounded-lg border p-3"><option value="">Gender</option><option value="male">Male</option><option value="female">Female</option><option value="other">Other</option></select><input name="education_status" placeholder="Education status" class="rounded-lg border p-3"><textarea name="interests" placeholder="Interests" class="rounded-lg border p-3"></textarea><textarea name="needs" placeholder="Current needs" class="rounded-lg border p-3"></textarea><textarea name="success_story" placeholder="Success story" class="rounded-lg border p-3 md:col-span-2"></textarea><button class="rounded-lg bg-emerald-700 px-5 py-3 font-semibold text-white">Add child profile</button></form>
<div class="overflow-x-auto rounded-2xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="bg-slate-50"><tr><th class="p-4">Code</th><th>Age</th><th>Education</th><th>Status</th><th class="p-4">Actions</th></tr></thead><tbody>@forelse($children as $c)<tr class="border-t align-top"><td class="p-4 font-semibold">{{$c->public_code}}</td><td>{{$c->age ?? '—'}}</td><td>{{$c->education_status ?? '—'}}</td><td>{{$c->active?'Active':'Archived'}}</td><td class="p-4"><details><summary class="cursor-pointer text-emerald-700">Edit</summary><form method="POST" action="{{route('admin.children.update',$c)}}" class="mt-3 grid gap-2 min-w-[260px]">@csrf @method('PUT')<input name="public_code" value="{{$c->public_code}}" required class="rounded border p-2"><input name="age" type="number" min="0" max="18" value="{{$c->age}}" class="rounded border p-2"><select name="gender" class="rounded border p-2"><option value="">Gender</option><option value="male" @selected($c->gender==='male')>Male</option><option value="female" @selected($c->gender==='female')>Female</option><option value="other" @selected($c->gender==='other')>Other</option></select><input name="education_status" value="{{$c->education_status}}" class="rounded border p-2"><textarea name="interests" class="rounded border p-2">{{$c->interests}}</textarea><textarea name="needs" class="rounded border p-2">{{$c->needs}}</textarea><textarea name="success_story" class="rounded border p-2">{{$c->success_story}}</textarea><label><input type="checkbox" name="active" value="1" @checked($c->active)> Active</label><button class="rounded bg-slate-900 px-3 py-2 text-white">Update</button></form></details>@if($c->active)<form method="POST" action="{{route('admin.children.destroy',$c)}}" class="mt-2">@csrf @method('DELETE')<button class="text-red-600">Archive</button></form>@else<form method="POST" action="{{route('admin.children.restore',$c)}}" class="mt-2">@csrf @method('PATCH')<button class="text-emerald-700">Restore</button></form>@endif</td></tr>@empty<tr><td colspan="5" class="p-8 text-center text-slate-500">No child profiles yet.</td></tr>@endforelse</tbody></table></div>{{$children->links()}}</div>
@endsection
