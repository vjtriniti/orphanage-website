@extends('layouts.admin')
@section('title','Volunteers')
@section('content')
<div class="space-y-6"><div><h1 class="text-2xl font-bold">Volunteers</h1><p class="text-slate-500">Review applications, edit volunteer details and manage approval.</p></div>@if(session('success'))<div class="rounded-xl bg-emerald-50 p-4 text-emerald-700">{{session('success')}}</div>@endif
<div class="overflow-x-auto rounded-2xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="bg-slate-50"><tr><th class="p-4">Volunteer</th><th>Email</th><th>Skills</th><th>Status</th><th class="p-4">Actions</th></tr></thead><tbody>@forelse($volunteers as $v)<tr class="border-t align-top"><td class="p-4 font-semibold">{{$v->name ?: ($v->user->name ?? '—')}}<div class="text-slate-500">{{$v->phone}}</div></td><td>{{$v->email ?: ($v->user->email ?? '—')}}</td><td class="max-w-xs">{{$v->skills ?: '—'}}</td><td><form method="POST" action="{{route('admin.volunteers.status',$v)}}" class="flex gap-2">@csrf @method('PATCH')<select name="status" class="rounded border p-2"><option value="pending" @selected($v->status==='pending')>Pending</option><option value="approved" @selected($v->status==='approved')>Approved</option><option value="rejected" @selected($v->status==='rejected')>Rejected</option></select><button class="rounded bg-emerald-700 px-3 py-2 text-white">Save</button></form></td><td class="p-4"><details><summary class="cursor-pointer text-emerald-700">Edit</summary><form method="POST" action="{{route('admin.volunteers.update',$v)}}" class="mt-3 grid min-w-[280px] gap-2">@csrf @method('PUT')<input name="name" value="{{$v->name}}" placeholder="Name" required class="rounded border p-2"><input name="email" type="email" value="{{$v->email}}" placeholder="Email" required class="rounded border p-2"><input name="phone" value="{{$v->phone}}" placeholder="Phone" class="rounded border p-2"><textarea name="skills" placeholder="Skills" class="rounded border p-2">{{$v->skills}}</textarea><textarea name="experience" placeholder="Experience" class="rounded border p-2">{{$v->experience}}</textarea><textarea name="availability" placeholder="Availability" class="rounded border p-2">{{$v->availability}}</textarea><input name="emergency_contact" value="{{$v->emergency_contact}}" placeholder="Emergency contact" class="rounded border p-2"><select name="status" class="rounded border p-2"><option value="pending" @selected($v->status==='pending')>Pending</option><option value="approved" @selected($v->status==='approved')>Approved</option><option value="rejected" @selected($v->status==='rejected')>Rejected</option></select><button class="rounded bg-slate-900 px-3 py-2 text-white">Update</button></form><form method="POST" action="{{route('admin.volunteers.destroy',$v)}}" class="mt-2">@csrf @method('DELETE')<button class="text-red-600" onclick="return confirm('Delete this volunteer?')">Delete</button></form></details></td></tr>@empty<tr><td colspan="5" class="p-8 text-center text-slate-500">No volunteer applications.</td></tr>@endforelse</tbody></table></div>{{$volunteers->links()}}</div>
@endsection
