@extends('layouts.admin')
@section('title','Settings')
@section('content')<div class="mx-auto max-w-3xl space-y-6"><div><h1 class="text-2xl font-bold">Organization Settings</h1><p class="text-slate-500">Update the public organization details.</p></div>@if(session('success'))<div class="rounded-xl bg-emerald-50 p-4 text-emerald-700">{{session('success')}}</div>@endif<form method="POST" action="{{route('admin.settings.update')}}" class="space-y-4 rounded-2xl bg-white p-6 shadow-sm">@csrf @method('PUT')<input name="organization_name" value="{{$settings['organization_name'] ?? ''}}" placeholder="Organization name" class="w-full rounded-lg border p-3"><input name="email" type="email" value="{{$settings['email'] ?? ''}}" placeholder="Email" class="w-full rounded-lg border p-3"><input name="phone" value="{{$settings['phone'] ?? ''}}" placeholder="Phone" class="w-full rounded-lg border p-3"><textarea name="address" placeholder="Address" class="w-full rounded-lg border p-3">{{$settings['address'] ?? ''}}</textarea><input name="currency" value="{{$settings['currency'] ?? 'NGN'}}" placeholder="Currency" class="w-full rounded-lg border p-3"><button class="rounded-lg bg-emerald-700 px-6 py-3 font-semibold text-white">Save settings</button></form></div>@endsection
