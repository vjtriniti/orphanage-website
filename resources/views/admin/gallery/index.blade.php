@extends('layouts.admin')
@section('title','Gallery')
@section('content')<div class="space-y-6"><div><h1 class="text-2xl font-bold">Gallery</h1><p class="text-slate-500">Create albums and upload approved images.</p></div>@if(session('success'))<div class="rounded-xl bg-emerald-50 p-4 text-emerald-700">{{session('success')}}</div>@endif<form method="POST" action="{{route('admin.gallery.store')}}" enctype="multipart/form-data" class="grid gap-3 rounded-2xl bg-white p-5 shadow-sm md:grid-cols-2">@csrf<input name="title" required placeholder="Gallery title" class="rounded border p-3"><input name="cover_image" type="file" accept="image/*" class="rounded border p-3"><textarea name="description" placeholder="Description" class="rounded border p-3 md:col-span-2"></textarea><button class="rounded bg-emerald-700 px-5 py-3 text-white">Create gallery</button></form><div class="grid gap-4 md:grid-cols-2">@forelse($galleries as $g)<div class="rounded-2xl bg-white p-5 shadow-sm"><h2 class="font-bold">{{$g->title}}</h2><p class="text-sm text-slate-500">{{$g->images->count()}} images</p><form method="POST" action="{{route('admin.gallery.images.upload',$g)}}" enctype="multipart/form-data" class="mt-4 flex gap-2">@csrf<input name="image" type="file" accept="image/*" required class="min-w-0 flex-1 rounded border p-2"><button class="rounded bg-emerald-700 px-3 py-2 text-white">Upload</button></form><div class="mt-4 grid grid-cols-3 gap-2">@foreach($g->images as $image)<div><img src="{{asset('storage/'.$image->path)}}" class="h-24 w-full rounded object-cover"><form method="POST" action="{{route('admin.gallery.images.destroy',$image)}}">@csrf @method('DELETE')<button class="text-xs text-red-600">Remove</button></form></div>@endforeach</div></div>@empty<div class="rounded-2xl bg-white p-8 text-center text-slate-500">No galleries yet.</div>@endforelse</div>{{$galleries->links()}}</div>@endsection
